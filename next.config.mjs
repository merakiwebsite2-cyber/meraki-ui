/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactStrictMode: true,
  images: {
     domains: ["storage.meraki-interiors.ae"],
  },
};

export default nextConfig;
